# Skill-Agent Control Plane Integration

The Legal Knowledge Runtime exposes legal document ingestion preflight, structure, retrieval planning, context-bundle, eval, integrity, and safety helpers as registered capabilities.

These capabilities are implemented here, but their lifecycle, rating, and workflow-composition metadata are governed by Semantic Substrate.
